# AxMenuIcon widget

Mendix pluggable widget that renders an MDI icon by name and executes an action on click.

## Properties

- `iconName` (string): icon key from `@mdi/js`, e.g. `mdiApps`
- `onClick` (action): action executed when the icon is clicked
