# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Account {
  avatar: String
  cdnAvatarId: String
  cdnOwnerId: String!
  createdAt: DateTime
  display: String!
  email: String!
  phone: String
  status: AccountStatus!
  updatedAt: DateTime
  username: String!
  uuid: ID!
}

enum AccountStatus {
  ACTIVE
  CLOSED
  LOCKED
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  """
  Invalidates the session with the given token. Returns true if a session was removed.
  """
  removeSession(token: String!): Boolean!

  """Updates an account. Throws when the uuid does not exist."""
  updateAccountInfo(input: UpdateAccountInfoInput!, uuid: ID!): Account!
}

type Query {
  """Returns the account record by uuid, or null if missing."""
  accountInfo(uuid: ID!): Account

  """
  Returns accounts (newest first). Optionally filter by status; paginate with limit/skip.
  """
  accounts(limit: Int = 50, skip: Int = 0, status: AccountStatus): [Account!]!

  """Returns the session by token, or null if missing/expired."""
  session(token: String!): Session
}

type Session {
  account: SessionAccount!
  createdAt: DateTime
  expiresAt: DateTime!
  token: String!
  uuid: ID!
}

type SessionAccount {
  avatar: String
  display: String!
  email: String!
  phone: String
  status: AccountStatus!
  username: String!
  uuid: ID!
}

input UpdateAccountInfoInput {
  avatar: String
  cdnAvatarId: String
  display: String
  email: String
  phone: String
}