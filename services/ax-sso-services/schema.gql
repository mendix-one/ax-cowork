# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  """
  Invalidates the session with the given token. Returns true if a session was removed.
  """
  removeSession(token: String!): Boolean!

  """Updates a user. Throws when the uuid does not exist."""
  updateUserInfo(input: UpdateUserInfoInput!, uuid: ID!): User!
}

type Query {
  """Returns the session by token, or null if missing/expired."""
  session(token: String!): Session

  """Returns the user record by uuid, or null if missing."""
  userInfo(uuid: ID!): User

  """
  Returns users (newest first). Optionally filter by status; paginate with limit/skip.
  """
  users(limit: Int = 50, skip: Int = 0, status: UserStatus): [User!]!
}

type Session {
  createdAt: DateTime
  expiresAt: DateTime!
  token: String!
  user: SessionUser!
  uuid: ID!
}

type SessionUser {
  avatar: String
  display: String!
  email: String!
  phone: String
  status: UserStatus!
  username: String!
  uuid: ID!
}

input UpdateUserInfoInput {
  avatar: String
  cdnAvatarId: String
  display: String
  email: String
  phone: String
}

type User {
  avatar: String
  cdnAvatarId: String
  cdnOwnerId: String!
  createdAt: DateTime
  display: String!
  email: String!
  phone: String
  status: UserStatus!
  updatedAt: DateTime
  username: String!
  uuid: ID!
}

enum UserStatus {
  ACTIVE
  CLOSED
  LOCKED
}