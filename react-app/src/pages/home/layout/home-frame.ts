// The empty left/right rails are sized to the bottom-bar height, so the EPS/MPS shell frames the landing
// page with a thin, symmetric gutter (top bar + side gutters + footer). Shared by HomeLayoutLeft /
// HomeLayoutRight / HomeLayoutBottom so the rail width and footer height stay in lockstep.
export const HOME_FRAME = '0.5rem'
